# subsembly

## Setup

Install dependencies:
```
yarn install
```

## Compile

```
cd subsembly-cli
yarn dev compile
```

## Generate Spec

```
yarn dev spec --to=./chain-spec.json
yarn dev spec --src=./chain-spec.json
```